/** @type {import("next").NextConfig} */
const config = {
  images: {
    domains: [
      "picsum.photos",
      "loremflickr.com",
      "51.120.120.77",
      "example.com",
    ],
  },
};

export default config;
